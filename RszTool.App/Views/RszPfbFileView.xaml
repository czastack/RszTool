<UserControl x:Class="RszTool.App.Views.RszPfbFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RszTool.App.Views"
             xmlns:viewmodels="clr-namespace:RszTool.App.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:RszPageViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary Source="TreeViewStyles.xaml" />
    </UserControl.Resources>

    <DockPanel>
        <TreeView>
            <TreeViewItem ItemsSource="{Binding Path=InstancesList.ListItems}" Header="Instances" />
        </TreeView>
    </DockPanel>
</UserControl>
