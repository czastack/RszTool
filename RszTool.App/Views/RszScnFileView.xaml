<UserControl x:Class="RszTool.App.Views.RszScnFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RszTool.App.Views"
             xmlns:viewmodels="clr-namespace:RszTool.App.ViewModels"
             xmlns:rsztool="clr-namespace:RszTool;assembly=RszTool"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ScnFileViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="TreeViewStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <viewmodels:ScnFolderDataSubItemsConverter x:Key="ScnFolderDataSubItemsConverter"/>
            <viewmodels:ScnGameObjectDataSubItemsConverter x:Key="ScnGameObjectDataSubItemsConverter"/>

            <HierarchicalDataTemplate DataType="{x:Type rsztool:ScnFile+FolderData}"
                                      ItemsSource="{Binding ., Converter={StaticResource ScnFolderDataSubItemsConverter}}">
                <TextBlock Text="{Binding Path=Name}" Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <TextBlock.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Paste Gameobject" Command="{Binding PlacementTarget.Tag.PasetGameObjectToFolder, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}" />
                        </ContextMenu>
                    </TextBlock.ContextMenu>
                </TextBlock>
            </HierarchicalDataTemplate>

            <HierarchicalDataTemplate DataType="{x:Type rsztool:ScnFile+GameObjectData}"
                                      ItemsSource="{Binding ., Converter={StaticResource ScnGameObjectDataSubItemsConverter}}">
                <TextBlock Text="{Binding Path=Name}" Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <TextBlock.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="_Copy" Command="{Binding PlacementTarget.Tag.CopyGameObject, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}" />
                            <MenuItem Header="Remove" Command="{Binding PlacementTarget.Tag.RemoveGameObject, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}" />
                            <MenuItem Header="Duplicate" Command="{Binding PlacementTarget.Tag.DuplicateGameObject, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}" />
                            <MenuItem Header="Paste Gameobject to children" Command="{Binding PlacementTarget.Tag.PasetGameObjectToParent, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}" />
                        </ContextMenu>
                    </TextBlock.ContextMenu>
                </TextBlock>
            </HierarchicalDataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox Text="{Binding FilePath, Mode=OneWay}" IsReadOnly="True" Margin="0 0 0 5" Grid.Row="0" />
        <TreeView Grid.Row="1">
            <TreeViewItem ItemsSource="{Binding Path=Folders}" Header="Folders" />
            <TreeViewItem ItemsSource="{Binding Path=GameObjects}" Header="GameObjects">
                <TreeViewItem.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="_Paste GameObject" Command="{Binding PasetGameObject}" />
                    </ContextMenu>
                </TreeViewItem.ContextMenu>
            </TreeViewItem>
        </TreeView>
    </Grid>
</UserControl>
